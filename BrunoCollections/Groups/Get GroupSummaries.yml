info:
  name: Get GroupSummaries
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/v1/api/groups/1/summary
  headers:
    - name: X-User-Id
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
