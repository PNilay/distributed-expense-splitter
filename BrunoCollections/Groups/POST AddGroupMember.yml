info:
  name: POST AddGroupMember
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/v1/api/groups
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Weekend",
        "description": "description",
        "createdBy":2
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
