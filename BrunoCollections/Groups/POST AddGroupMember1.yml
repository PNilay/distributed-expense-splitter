info:
  name: POST AddGroupMember1
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/groups
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Trip to USA test",
        "description": "description",
        "createdBy":3
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
