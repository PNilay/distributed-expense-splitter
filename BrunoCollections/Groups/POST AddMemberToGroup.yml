info:
  name: POST AddMemberToGroup
  type: http
  seq: 1

http:
  method: post
  url: http://localhost:8080/v1/api/groups/1/members
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userId": 3
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
