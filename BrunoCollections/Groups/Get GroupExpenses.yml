info:
  name: Get GroupExpenses
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/v1/api/groups/summaries
  headers:
    - name: X-User-Id
      value: "2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
