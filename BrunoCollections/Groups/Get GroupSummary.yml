info:
  name: Get GroupSummary
  type: http
  seq: 9

http:
  method: GET
  url: /groups/{groupId}/summary
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
