info:
  name: DashBoardBalanceByUserID
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/v1/api/users/1/balance-summary
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
